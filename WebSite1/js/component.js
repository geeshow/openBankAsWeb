var util = function () {
    
}
util.mask = function (value, pattern) {
    var cntY = 0;
    var patternValue;
    for (var i = 0 ; i < pattern.length ; i++) {
        if ("Y" == pattern.charAt(i)) {

        }
    }
}